20240412_美团_后端一面
1.项目
2.中间件了解哪些？
3.消息队列要素？
消息，生产者，消费者，队列，消息传递模式，消息确认机制，消息持久化，消息传输协议。
4.数据库引擎？
  除了 InnoDB 外，常见的数据库引擎还有：
  1. **MyISAM**：
     - MyISAM 是 MySQL 中的另一个常见的数据库引擎。它以性能为优先，适用于对读操作频繁、写操作相对较少的场景。MyISAM 不支持事务和行级锁，但支持全文索引等功能。
  2. **Memory**：
     - Memory 引擎（也称为 HEAP 引擎）将数据存储在内存中，而不是磁盘上。它适用于对速度要求非常高的临时表或缓存表，但是一旦数据库重启或崩溃，其中的数据将会丢失。
  3. **Archive**：
     - Archive 引擎用于存储和检索大量的归档数据。它以非常高的压缩比存储数据，并支持仅插入和检索操作，不支持更新或删除操作。
  4. **CSV**：
     - CSV 引擎用于读取和写入 CSV 文件格式的数据。它允许将 CSV 文件视为数据库表，并支持对 CSV 文件的查询操作。
  5. **NDB Cluster**：
     - NDB Cluster（也称为 NDB）是 MySQL 中的一个分布式数据库引擎，专门用于构建高可用性和高可扩展性的分布式数据库集群。它支持自动分区和数据复制，以及事务和并发控制等功能。
  6. **Blackhole**：
     - Blackhole 引擎实际上是一个“空引擎”，它会接收写入操作但不会保存数据。它通常用于将数据写入到日志文件或触发特定的事件。  
  7. **Federated**：
     - Federated 引擎允许 MySQL 服务器连接到其他 MySQL 服务器上的表，并将其视为本地表。这使得可以在多个数据库服务器之间执行查询操作，实现分布式查询和数据集成。

  以上是 MySQL 中的一些常见的数据库引擎，每种引擎都有其特定的用途和优缺点，根据具体的业务需求和性能要求选择合适的引擎是很重要的。
5.索引种类？
6.索引失效？
7.聚簇索引和非聚簇索引？
8.事物隔离级别？
9.MVCC怎么实现？
10.死锁？
11.数据库死锁是什么情况？
12.写代码，出现死锁？
13.一个简单题
